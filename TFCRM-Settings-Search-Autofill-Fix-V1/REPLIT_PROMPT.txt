طبّق Patch: TFCRM-Settings-Search-Autofill-Fix-V1

Repository:
https://github.com/mohamedamouseo-a11y/tfcrm-patchs

Patch folder:
TFCRM-Settings-Search-Autofill-Fix-V1

Target project:
/var/www/TFCRM

نفّذ فقط:
1) cd /tmp && rm -rf tfcrm-patchs && git clone --depth 1 https://github.com/mohamedamouseo-a11y/tfcrm-patchs.git
2) cd /tmp/tfcrm-patchs/TFCRM-Settings-Search-Autofill-Fix-V1
3) python3 APPLY_PATCH.py /var/www/TFCRM
4) cd /var/www/TFCRM
5) npm run check

بعد التطبيق اختبر يدويًا:
- افتح /settings.
- اعمل Reload للصفحة.
- تأكد أن الإيميل المحفوظ لا يظهر داخل Search Settings.
- اضغط داخل Search واكتب أي كلمة وتأكد أن الفلترة مازالت تعمل طبيعي.

ممنوع أي refactor أو تعديلات خارج:
- client/src/pages/AdminSettings.tsx

ممنوع:
- db:push
- migrations
- schema changes
- تعديل Smart Search Phase 1
- تعديل permissions/routes
- restart لـ Evolution/WhatsApp

لو npm run check فشل بسبب Errors موجودة مسبقًا، لا تصلحها تلقائيًا. ارجع بعدد الأخطاء قبل/بعد إن أمكن، ووضّح هل ظهر Error جديد متعلق بالملف المعدل أم لا.

الرد النهائي:
PASS/FAIL:
PATCH_APPLIED:
AUTOFILL_AFTER_RELOAD:
SEARCH_TYPING:
CHECK:
FILES_CHANGED:
DB_CHANGED:
NOTES:
